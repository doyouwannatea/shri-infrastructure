/**
 * @param {import('express').Request} req 
 * @param {import('express').Response} res 
 */
module.exports = (req, res) => {
    // TODO: написать ручку для /notify-build-result
    res.send('notify build result')
}